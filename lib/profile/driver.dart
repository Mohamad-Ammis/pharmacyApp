import 'package:flutter/material.dart';

class driverr extends StatelessWidget {
  const driverr({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(left: 24, right: 24),
      child: Divider(
        color: Colors.black12,
        height: 1,
      ),
    );
  }
}
